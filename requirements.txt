# Dependencies of the case pipeline.
#
# These are what the scripts actually import — not an aspirational list.
# Check with:  grep -rhE "^(import|from) " notebooks/*.py | sort -u
#
# Install:  pip install -r requirements.txt

duckdb          # the analysis engine: procesar · analizar · verificar · graficos
numpy           # graficos  (interpolating two curves onto a common grid)
matplotlib      # graficos · estilo
python-docx     # build_docx.py  (imported as `docx`)

# No pandas: the aggregation happens in SQL, so nothing here ever builds a DataFrame.
# It came from the template and was removed after checking the imports above.
